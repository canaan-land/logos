<Window x:Class="Logos.DisplayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Logos"
        mc:Ignorable="d"
        Title="DisplayWindow" Height="450" Width="800" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" WindowStartupLocation="CenterScreen" WindowState="Maximized" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel>
        <local:OutlinedTextBlock x:Name="DisplayText" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Text="{Binding Path=TextString}"
                   FontFamily="{Binding Path=TextFont}"
                   FontSize="{Binding Path=TextFontSize}"
                   FontWeight="{Binding Path=TextFontBold, Converter={local:BoolToBoldConverter}}"
                   FontStyle="{Binding Path=TextFontItalic, Converter={local:BoolToItalicConverter}}"
                   TextDecorations="{Binding Path=TextFontUnderline, Converter={local:BoolToUnderlineConverter}}"
                   Fill="{Binding Path=TextFontColor}" Cursor="SizeAll">
            <local:OutlinedTextBlock.Style>
                <Style TargetType="local:OutlinedTextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=TextOutline}" Value="True">
                            <Setter Property="Stroke" Value="{Binding Path=TextOutlineColor}"/>
                            <Setter Property="StrokeThickness" Value="{Binding Path=TextOutlineWidth}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=TextOutline}" Value="False">
                            <Setter Property="StrokeThickness" Value="0"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </local:OutlinedTextBlock.Style>
        </local:OutlinedTextBlock>
    </StackPanel>
</Window>
